EXPO_PUBLIC_REVENUECAT_IOS_API_KEY=appl_YOUR_KEY_HERE
EXPO_PUBLIC_PREMIUM_BYPASS_EMAILS=you@example.com,friend@example.com

# Haber & piyasa (Supabase Edge Functions secrets — EXPO .env'de değil):
# FINNHUB_API_KEY=your_finnhub_token
# GEMINI_API_KEY=Moono günlük özet + moonogemini için (Edge secrets)

# Moono günlük özet cache tablosu: supabase/migrations/...moono_daily_briefs.sql
# → `npx supabase db push` veya SQL Editor'de çalıştır, sonra:
# `npx supabase functions deploy moono-daily-brief`
